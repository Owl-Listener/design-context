# Design decisions — PROJECT_NAME

Append-only. Newest first. Never edit or delete an entry: reversals are new entries that supersede old ones. See design-context SPEC.md section 3 for the format rules.

<!-- Template entry, copy and fill:

## YYYY-MM-DD — Short decision title
**Decision:** The decision, stated so it can be applied without asking.
**Because:** The reason. Required. This is what lets the decision generalise.
**Instead:** What happens in place of the banned thing. For prohibitions only.
**Supersedes:** none | date + title of the entry this replaces
**Status:** settled | provisional

Instead: is how a ban avoids creating a new ambiguity. "No celebration animations"
settles one case and silently opens another — so what does finishing a book look
like? — and whoever is next at the keyboard will answer it for you. If you don't
know yet, say so in the field:

**Instead:** open — nobody has hit the case yet. Decide it when it comes up.

That tells the next agent it may propose and must ask. A missing Instead tells it
nothing, so it guesses.

-->
